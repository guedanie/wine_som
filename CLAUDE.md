# Somm — Wine Recommendation App

## What This Is
Full-stack wine recommendation app. Users enter zip code + budget + style preferences and get Claude-powered sommelier recommendations for wines available at local retailers near them. (Public overview: `README.md`. Frontend design system: `frontend/CLAUDE.md`.)

**LIVE (private beta since 2026-07-05):** frontend `wine-som-pkwz-chi.vercel.app` (Vercel) + backend `winesom-production.up.railway.app` (Railway) + Supabase. Installable PWA. Testers in San Antonio, Austin, Nashville, Charlotte + Winston-Salem NC; Dallas focus coming.

## Tech Stack
- **Database + Auth**: Supabase (cloud project `knpldhksfsetujbcfrsj`)
- **Backend**: Python 3.9, FastAPI, supabase-py
- **Frontend**: React 19 + Vite + Tailwind v3 (desktop + mobile/PWA)
- **AI**: Anthropic Claude — Sonnet 4.6 (recommendations, streamed) + Haiku 4.5 (Somm overlay chat, NL intent parse); fact extraction runs on the mini's local qwen
- **Scraping**: urllib (Shopify), curl subprocess (HEB/CM GraphQL, Spec's, City Hive), OAuth REST (Kroger) — no Playwright
- **Geo**: `pgeocode` (offline US zip centroids)
- **Hosting**: Vercel (web) + Railway (api) + Supabase (db)

---

## Critical gotchas (read before working)

- **Python 3.9.6** — use `Optional[str]` from `typing`, NOT `str | None` (that needs 3.10+).
- **Run backend commands from `backend/`** so `../.env` resolves. **Run frontend commands (vitest/vite) from `frontend/`** so config loads — a common failure is running from the repo root, which yields "test is not defined" / "cannot resolve index.html" (cwd drift).
- **Supabase**: anon key = public reads; service_role = backend only (bypasses RLS). Tables need explicit GRANTs (`supabase/migrations/20260602000002_grants.sql`). User tables (`profiles`, `favorites`, `cellar`, `feedback`) are RLS-scoped `user_id = auth.uid()` and read/written directly via supabase-js — no backend endpoint.
- **`retail_inventory` → `stores` FK is `store_ref`** (UUID), NOT `store_id`.
- **Wine identity = `wines.upc_canonical`** (full UNIQUE constraint). `canonical_upc()` normalizes real barcodes; any id with a letter (synthetic `shopify-…`, `twinliquors-…`) passes through unchanged. Scrapers upsert `on_conflict="upc_canonical"`; `retail_inventory.upc` stores the RAW barcode.
- **`.env` is not in git** — copy it manually to any new machine (e.g. the mini). Prod (Vercel) needs `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` baked at build time or the sign-in UI silently hides.
- **TDD** the logic-bearing code; when a scorer/parse test passes for the "wrong reason" (stable-sort ties), reorder inputs so only the change under test can pass.
- **Frontend tests + `localStorage`**: Node 22+ ships a native `localStorage` that's DISABLED without `--localstorage-file`, so under vitest's jsdom env `globalThis.localStorage` is `undefined` and any test that touches it throws `Cannot read properties of undefined`. `frontend/src/test/setup.js` installs a fresh in-memory Storage per test (fixed 2026-08-24, item 43); per-file `vi.stubGlobal('localStorage', …)` still overrides it. If you see that error, don't re-stub in every file — the global one is already there.

## Running it

```bash
# Backend (from backend/)          Frontend (from frontend/)
cd backend                          cd frontend
python3 -m uvicorn api.main:app --reload    npm run dev        # localhost:5173
# localhost:8000  (docs: /docs)

# Tests
cd backend  && python3 -m pytest tests/ -m "not integration"   # fast, secret-less
cd frontend && npx vitest run
```

Seeding scrapers + the full per-retailer run commands: **`docs/reference/scrapers.md`**.

---

## Current status (summary)

Full component table + file map: **`docs/reference/build-log.md`**.

- **Retail data** — 11 scrapers live (H-E-B, Central Market, Spec's, Kroger, Harris Teeter, Twin Liquors, Pogo's, Geraldine's, AOC, US Natural Wine, Antonelli's, Harvest) across TX/TN/NC. Weekly GitHub cron runs 9 (**H-E-B + Central Market BROKEN since 2026-07-26 — Incapsula JS challenge, item 45; H-E-B benched, SA testers get no H-E-B**); **Spec's + Twin Liquors + Vivino + local-LLM extraction run on the residential-IP Mac mini** (datacenter IPs are Cloudflare/Vivino-blocked; Spec's silently blocked since 2026-07-01 — moved to the mini 2026-07-13). `scripts/verify_scrape_runs.py` runs after the weekly cron and flips silent-zero "successes" to failed + Slack-alerts. `scrapers/base.py` upserts retry on Postgres deadlock/serialization codes (`40P01`/`40001`, 4× backoff) — added after the mini Spec's run overlapped the GitHub scrape and deadlocked. Details: `docs/reference/scrapers.md`, `docs/mac-mini-enrichment-server.md`.
- **Enrichment** — layered: retailer data → Vivino (ratings/facts/image) → local LLM (qwen2.5:7b, Haiku-parity) → deterministic grape+region structure table. Details: `docs/reference/enrichment.md`.
- **Recommendation** — deterministic knowledge-based scorer shortlists; Claude (Sonnet 4.6) picks + narrates, streamed progressively (narrative word-by-word, then cards one at a time). Personalization (saved + cellar + 👍/👎 votes + taste-profile interview) re-ranks and is cited. Details: `docs/reference/recommendation.md`.
- **Frontend** — React SPA + installable PWA; anonymous-first with optional magic-link accounts (saved bottles, cellar, taste profile). Design system: `frontend/CLAUDE.md`.
- **Blocked retailers** — ~~**Total Wine**~~ (**UNLOCKED 2026-08-26, item 46** — never a challenge; plain HTTP reads it, the *automated browser* is what gets 403, and store scoping turned out to be a forgeable `twm-userStoreInformation` cookie. Remaining work is HTML product parsing, not access.), Whole Foods (Amazon auth — an auth wall, NOT a bot challenge, so the item-45 technique does not apply), Publix/Costco/Trader Joe's (Akamai; Costco is membership-gated and Trader Joe's has no e-commerce at all), Food Lion (Cloudflare), Tom Thumb/Albertsons (Incapsula — same vendor as H-E-B, so item 45's patchright path is the obvious thing to try), ~~Corkdorks/Frugal MacDoogal~~ (City Hive — UNBLOCKED 2026-08-01 via the Twin Liquors bypass; both confirmed scrapable, mini scraper queued in `docs/mini-agent-tasks.md`).
  **Blocks are not one thing.** Three distinct classes, and the fix for one is useless (or harmful) for another: a **JS challenge** (H-E-B — a headed patchright browser solves it), **browser fingerprinting** (Total Wine — plain HTTP passes, the browser is refused; reaching for patchright makes it *worse*), and an **auth wall** (Whole Foods — no technique gets you an account). Establish which class you're facing with a cheap urllib-vs-browser A/B before choosing a tool.

## Reference docs
These hold the deep detail moved out of this file — **consult the relevant one on demand when a task touches that system; don't read them preemptively.** The inline "Details: …" pointers above tell you which applies.

- `docs/reference/scrapers.md` — per-retailer API notes, UPC dedup, seeding commands
- `docs/reference/enrichment.md` — GrapeMinds, Vivino, local LLM, structure table
- `docs/reference/recommendation.md` — scorer, personalization, intent, zip→store
- `docs/reference/build-log.md` — full component status table + file map
- `docs/mac-mini-enrichment-server.md` — the residential-IP enrichment runbook
- `docs/mini-agent-tasks.md` — queued mini work: Spec's migration (+ its two known landmines) and extraction run-logging
- `docs/recommendation-architecture-audit.md` — **root diagnosis of the false-absence bug class** + the 9-item prioritized plan (items #1, #3–#9 still open)
- `docs/oracle-production-verification.md` / `docs/oracle-verification-round2.md` — the two adversarial red-team sweeps against the live deploy
- `docs/ios-app-store-roadmap.md` — Capacitor wrap path, Guideline 4.2 mitigation, phased plan
- `docs/product-family-key.md` — vintage-agnostic wine identity (future)
- `data/exploration/*_findings.md` — how each retailer API was reverse-engineered
- `frontend/CLAUDE.md` — design system (type, color, components, voice)

---

## API Keys (.env)

| Key | Status |
|---|---|
| `SUPABASE_URL` / `SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` | ✅ Set |
| `ANTHROPIC_API_KEY` | ✅ Set (monthly spend cap in console) |
| `GRAPEMINDS_API_KEY` | ✅ Set (~17/250 calls used) |
| `KROGER_CLIENT_ID` / `KROGER_CLIENT_SECRET` | ✅ Set — official Developer API (Kroger + Harris Teeter) |
| `ADMIN_TOKEN` | ✅ Set — gates `/api/enrich/*` in prod |
| `SLACK_WEBHOOK_URL` | ✅ Set — scrape/enrich notifications |
| `ALLOWED_ORIGINS` | ✅ Set on Railway — prod CORS |
| `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` / `VITE_POSTHOG_KEY` | ✅ Set in Vercel (build-time) |
| `WINE_SEARCHER_API_KEY` | ❌ Denied | · `VINERADAR_API_KEY` ⏳ waitlist · `APIFY_API_TOKEN` ⬜ · `INSTACART` ❌ |

---

## What's Next (priority order)

Done unless noted. Detailed history: `docs/reference/build-log.md`.

1–10. ✅ Sommelier routing · scheduled scrape · HEB CSV expansion · feedback loop · Somm overlay · deploy · mobile/PWA · structure table · chat naturalness · **Mac mini enrichment server** (Vivino + weekly extraction + Twin Liquors launchd, all live).

11. ✅ **Local LLM extraction cutover** — done 2026-07-10: initial 6,298-wine drain wrote 5,974 (~5% loss to malformed-UUID echoes, non-corrupting); persist_structure landed 11,899 new structure rows. Haiku extraction step removed from `weekly-scrape.yml` 2026-07-11. Mini's weekly `com.somm.extraction-enrich` LaunchAgent (Sun 03:00 CT) is now the only extraction path. Steady-state NULL: 16.9% varietal / 19.1% region / 10.9% both (of 19,254 wines).
12. ✅→⚙️ **Blend structure/sweetness LLM pass** — code landed + pushed 2026-07-19; full drain running overnight. `scripts/backfill_structure_llm.py` (qwen2.5:7b on the mini) fills the two gaps the deterministic grape table can't: LLM-inferred `sweetness` MERGED onto the ~17.5k profiles that lack it (body/tannins/acidity untouched — table/Vivino/GrapeMinds stay authoritative; marked `sweetness_source:'llm'`), and full 4-axis profiles for the ~3,400 unanchored blends the table can't map (`source:'llm'`). Precedence `vivino/grapeminds > table > llm`: `structure_to_persist` refreshes an llm profile from the table once grapes arrive, keeping the llm sweetness. Non-wine guard (shared `_is_non_wine` with the wine_type backfill) keeps sake/food out; echo-id validation drops qwen's malformed-UUID rows (idempotent — re-fetched next pass). Weekly extraction LaunchAgent chain runs it incrementally (`--limit 500`). Spot-check quality excellent (Apothic Red/Tawny Port/Sweet Muscadine sweetness spot-on, dry reds/Champagne=1). REMAINING: the ~21k drain finishing (sweetness coverage 6%→~90% expected) + final-number docs; optional cleanup pass for the ~17% blend malformed-UUID drops.
13. ✅→⚙️ **Extraction pass on new Nashville/NC/Dallas wines** — mostly done by the weekly mini job (varietal-null 2026-07-13: Harvest 4%, Harris Teeter 10%, Kroger 12% — vs H-E-B baseline 2%). REMAINING: **Pogo's Dallas residue** — 36% varietal-null, ~15% missing BOTH varietal+region (invisible to the recommender). These are extraction *failures*, not backlog (obscure fine-wine labels with no grape/region in the name — the 07-10 drain already tried them); re-running `--null-only` won't help. Fix path: Vivino matching from the mini (knows producers by name) — prioritize both-null wines in its queue. Matters for the Dallas push. **Queue prioritization landed 2026-07-14** (item 27): `run_vivino_sample.py` now fills each run's `VIVINO_LIMIT` (300/run on the mini) in tiers — both-null first, then un-enriched Bordeaux/Rhône, then the rest — so Pogo's residue drains at the weekly cadence.
14. ✅ **User accounts Phases 0–4** — magic-link auth, saved favorites, mobile account home, cellar (drinking windows + drank-rating), taste-profile interview, price watches (2026-07-13, see item 16). REMAINING: the watch notifier. Roadmap: `docs/user-accounts-roadmap.md`.
15. ✅ **Feedback-as-scoring-signal** — votes carry `user_id` + RLS read; `buildTasteContext` folds 👍→liked / 👎→disliked; scorer boosts/penalizes. Niche remaining: merge anon session votes on sign-in (needs backend JWT verify).
16. **Price intelligence** — ⚙️ design handoff: `frontend/design-system/handoffs/price-intelligence/`. Phase 1 done (`price_history` + delta trigger). **Phase A done 2026-07-13**: `PriceMarker` chip (4 variants) + dossier price-context module (drop/steady states, week-strip glyph, cheapest-row flag + struck was-price) fed by `utils/price_context.py` via `/api/wines/{id}`. **Phase B done 2026-07-13**: shortlist annotated with fresh drops (`fresh_drops_for`, one history fetch for the top-12), `price_drop` rides pick payloads, chip leads WineCard chip rows + mobile pick pill line, somm voices drops week-anchored ("dropped $5 at H-E-B this week"). **Phase C done 2026-07-13**: `GET /api/deals` (fresh drops via `fresh_price_drops` SQL window fn — postgrest's 1000-row pages made client-side scanning lossy — ranked quality×movement with an editorial floor: ≥$7, rated wines ≥3.7★, unrated pass) + Discover rail ("Worth grabbing · Week of X", hidden on empty weeks) + `/deals` screen. NOTE: `price_history` needed a read GRANT + RLS policy (migrations 20260713000001-3, applied) — anon silently saw 0 rows, so Phase A was steady-only in prod until then. **Phase D done 2026-07-13**: `price_watches` table (RLS like favorites, migration 20260713000004 applied) + Watch price ghost→Watching button in the dossier price module (both layouts) + anonymous tap → contextual sign-in nudge ("I'll tell you when X drops") with pending-watch intent through the magic-link round-trip. REMAINING: the notifier (weekly job joining `price_watches` × `fresh_price_drops` → PWA push/email — copy is in the design handoff). NOTE: restock variant needs a trigger extension (in_stock-only flips write no history row).
17. ✅ **Ratings badge** in WineCard + mobile pick messages.
18. ✅ **Per-pick distance** — `distance_miles` (user-zip centroid → store, haversine) flows recommend endpoint → pick → mobile store pill + desktop WineCard retailer line. Also fixed: Spec's + Twin Liquors scrapers now write `stores.address` (street was parsed then dropped; every other retailer already had it).
19. **Analytics** — ⚙️ foundation shipped; PostHog live in Vercel.
20. ⚙️ **Kroger banner expansion** — Memphis/Houston/other NC-VA cities (config-only via `MARKETS`). **Nashville deepened 2026-08-01**: 4→35 stores (urban core + Franklin/Brentwood/Hendersonville/Mt Juliet/Smyrna suburban ring; IDs from a Locations-API sweep of 12 area zips, fuel/pharmacy excluded) — fixes thin tester inventory AND the empty-app problem for suburban zips outside the old 4-store 10-mi radius. Seeded immediately (not just queued for the weekly cron) — **run completed 2026-08-01: 32,275 products across all 35 stores, `failed: []`; Nashville now 36 stores (35 Kroger + Harvest), 25,702 in-stock rows** (was 5 stores / ~3.3k). Suburban zips that saw an empty app now have coverage (Brentwood 37027 → 15 nearby stores, Hendersonville 37075 → 10, Mt Juliet 37122 → 8, Franklin 37067 → 7). Frugal MacDoogal + Corkdorks (Nashville City Hive, 3 stores) bypass confirmed same day → mini scraper queued (`docs/mini-agent-tasks.md`).
21. Local MCP server for Claude Desktop (parked — memory `mcp-desktop-parked`).
22. More Shopify wine shops (zero new code) · enumerate full ~90 Twin Liquors stores.
23. Blocked probes (headless-browser only): Total Wine, WFM, Publix, Food Lion, Tom Thumb/Albertsons.
24. ✅ **Twin Liquors scraper** — live on the mini (Sun 04:00 CT). GitHub IPs Cloudflare-blocked. **Reusable: the api_key + client_origin bypass likely unblocks Corkdorks / Frugal MacDoogal (Nashville).**
25. **Personalization follow-ons (parked)** — anon-vote merge; `similar_to` badge in pick UI; consumed-cellar as taste signal; profile avoids as mechanical scorer penalties; smarter top-10 for the "[Your wines]" block.
26. ⚙️ **Bottle photo identification (Bottle Scan)** — user snaps a bottle label; app identifies the wine → (a) where to buy it locally or (b) one-tap save/cellar. **Design DONE 2026-07-23**: handoff bundle in `frontend/design-system/handoffs/bottle-scan/` (brief, `PHOTO-GUIDELINES.md` test-set spec, README spec + hi-fi prototype) — five-state `scanResult` contract (`exact`/`candidates`/`vintage_mismatch`/`unstocked`/`unreadable`), tab-bar camera FAB + chat entry, barcode fast-path (54% of catalog has a real UPC) + label-photo universal path. **Vision match-rate spike DONE 2026-07-23**: two-stage pipeline built + unit-tested — `enrichment/label_scan.py` (Claude vision, strict-JSON evidence-gated label read, Haiku+Sonnet) → `recommendation/label_match.py` (reuses item-31 name tokenizers; scores + classifies into the five states, pure/10-test). Harness `scripts/spike_vision_match.py` grades TOP1/TOP4/WRONG-CONF/MISS/DECLINE in `--smoke` (Vivino catalog-image ceiling) and `--photos` (real phone shots) modes. Smoke run (8 imgs): haiku 25% top1 / 75% top4+, sonnet 38% / 75%, **zero wrong-confident** on either, ~1.4s/2.4s median. REMAINING before a build: (1) the **real-photo run** for the honest number + threshold tuning (collect ~30 photos per `PHOTO-GUIDELINES.md` — that sets the exact-vs-candidates `confidence` split); (2) a `/api/identify` endpoint wrapping the matcher (its core is already the production identify logic); (3) frontend per the handoff.
27. ✅ **Old-World scoring gap (Bordeaux/Rhône) + extraction hallucinations** — gazetteer + evidence gate + appellation-law blend defaults shipped 2026-07-13; the backfills + both residue sweeps landed 2026-07-14 from the mini (details: `docs/mini-agent-tasks.md` Task 3). Regions are now clean: Portuguese retype done, Rhône fragments normalized, `scripts/revalidate_regions.py` applied 2,195 positive fixes, Rhône residue 72→0 real errors (15 hand-fixed), Bordeaux residue 151→73 all-genuine (71 hand-fixed incl. a DRC La Tâche); gazetteer hardened (conflict guards, single-word château needles need a Chateau word, 'St.'→Saint folding, Bdx/CdR shorthand). **Bulk NULLING stays deferred** — the gate would null 3,804 mostly-correct producer-knowledge regions (Grgich Hills→Napa class). **Grapes backfill DONE 2026-07-14**: `default_grapes_for()` is now color-aware (gated by `wine_type`; dual-color appellations like Graves/Pessac-Léognan/Hermitage require an explicit type) with an expanded table (right-bank satellites, northern-Rhône crus, Condrieu Viognier, Tavel rosé) plus red-only region-level fallback (`default_grapes_for_region`); `scripts/backfill_grapes.py` filled 375 of 491 grapes-empty Bordeaux/Rhône rows (34 trusted varietals, 326 appellation blends, 15 region-level) — Bordeaux grapes-empty 33%→7.4%, Rhône 36%→9.7%; 116 left for Vivino (unknown-type Pessac-Léognan, legit rosés/whites in red appellations, long-tail appellations). Vivino queue now prioritizes both-null wines → un-enriched Bordeaux/Rhône → rest, and may replace a law-default blend with real Vivino grapes (multi-grape defaults only, never a single scraped/inferred grape). **Law-regions extension DONE 2026-07-15**: `_REGION_DEFAULT_RULES` extended color-aware to 6 regions (Champagne, Douro, Penedès, Provence rosé, on top of Bordeaux/Rhône red) plus new appellations (Sangiovese DOCGs, Carmignano, Cava, Bandol, Blanc de Blancs), governed by the standing conservative rule (hard law + explicitly-approved conventions only — Bolgheri/Madeira/Cassis/Tuscany-region deliberately excluded, White Port name-guarded off the red Douro trio). Production backfill filled 356 of 628 grapes-empty law-region rows; catalog-wide grapes-empty now 22.7% (was 24.5%). REMAINING: optional scorer region-fallback only.
28. ✅ **Progressive pick streaming** — fixed the "paragraph sits alone, cards arrive late" gap (measured 6.5s to first text + 4.4s narrative→cards). Three parts: `eager_input_streaming` on the tool (narrative truly streams, first text ~1s), per-pick SSE `pick` events as each object closes (first card ~1.5s after narrative), slim pick schema (model sends only `wine_id`+`why`; name/price/retailer re-attached from candidates). Details: `docs/reference/recommendation.md` (Streaming pipeline).
29. ✅ **Candidate-fetch fix — intent-aware targeted fetch + type gate** — fixed the unordered/intent-blind 500-row breadth fetch dropping matches before scoring (e.g. "Bordeaux blend at Lincoln Heights" wrongly read as no match). Targeted region/store fetch merges into the pool, resolved-type hard gate (`candidate_filters.py`) replaces the raw wine_type filter, fuzzy `detect_store` boosts named-store matches. Verified end-to-end for Bordeaux + Rhône. Details: `docs/reference/recommendation.md`.
30. ✅ **NULL-`wine_type` backfill** — landed 2026-07-18, same shape as the grapes backfill (item 27). `infer_wine_type` hardened (`utils/__init__.py`): NFKD accent-fold (`_fold`) resolves accented varietals (Mourvèdre, Grüner Veltliner); grape vocabulary synced to every `reference.CORE_GRAPES` entry with a drift-guard test; sparkling method terms added (Pet Nat, Col Fondo, Franciacorta, Lambrusco — fixes "Zinfandel Pet Nat" misreading red), over-broad "ancestral" token dropped; **Port/Sherry/Madeira now type `fortified` not `dessert`** (`FORTIFIED_TERMS` checked before `DESSERT_TERMS`, both `infer_wine_type` and `wine_type_for_appellation` agree — true dessert wines Sauternes/ice wine unaffected). New `wine_type_for_appellation(region, sub_region)` (`reference.py`): curated single-color/style appellation map (Chablis→white, Champagne→sparkling, Brunello→red, Sauternes→dessert, Port/Sherry→fortified); multi-color places (Burgundy villages, Bordeaux communes, bare regions) deliberately excluded. `scripts/backfill_wine_type.py`: fill-only precedence varietal→name→first-grape→appellation, plus a non-wine guard (`_is_non_wine`) skipping grocery/beverage catalog noise (sake, cocktails, cider, maple syrup) so it stays NULL instead of mistyped. **Live run**: 3,528 of 5,443 NULL-type wines filled (2,311 red, 996 white, 121 sparkling, 46 fortified, 26 rosé, 24 orange, 4 dessert) — catalog wine_type-NULL 27.5%→9.7%. Remainder (~1,915): signal-less producer-only listings → Vivino/LLM territory; non-wine catalog noise → item 32 purge. Fast suite 558→579 passing.
31. ✅ **Name-directed full-inventory fallback** — landed 2026-07-19. `wine_name` added to the Haiku `wine_intent` parse; `deep_fetch_reason` fires a deep fetch when a bottle is named OR when a concrete grape/region/type ask went unmet by the random-500 breadth sample. Named mode searches the full nearby inventory by `wines.name ilike` (budget IGNORED — a lookup shouldn't be hidden by the slider), ranks all-token matches first (`rank_name_matches`), and pins up to 3 to the front (`pin_named_matches`); weak-pool mode re-fetches by grape (jsonb `grapes.cs.[...]`) or region honoring budget. The deep fetch + Claude call moved INSIDE the SSE generator behind a themed `status` frame ("Looking deeper into the cellar…"), so the message only shows when we actually dig and the common request is untouched. Narrative confirms the found bottle or hedges when it's not stocked. Pure helpers in `candidate_filters.py` (tokenize/rank/reason/pin) unit-tested; acceptance `scripts/verify_name_fallback.py`.
32. ✅ **Purge irrelevant inventory** — code landed 2026-07-21; migration + purge applied to prod 2026-07-23. Canonical `enrichment/non_wine.py` (deny-list of clearly-non-wine markers — beer/sake/food/merchandise — deliberately excluding trap tokens bourbon/rum/brandy/water/soda/martini/stout that collide with real wines; `should_exclude` guarded by barrel-name + wine-signal (has varietal/grape) + allowlist so a real wine is never dropped). `wines.excluded_at`/`exclusion_reason` soft-delete columns (migration `20260721000001_wines_excluded.sql`). `scripts/purge_non_wine.py` (dry-run default, `--apply` writes, idempotent). Read-path filtering (`excluded_at IS NULL`) across recommend/search/deals/wines/region. Wine-adjacent (vermouth/sangria/wine-cocktails) intentionally KEPT. **Applied 2026-07-23**: migration `20260721000001` was already in the remote migration history (columns existed, 0 rows excluded); `purge_non_wine.py --apply` soft-deleted **485 rows** (sake 163 incl. junmai/ginjo/nigori, syrup-family 181 incl. maple/pancake, beer/ale/IPA 42, fruit-cocktail/cocktails 38, NA-beverages ~45, small tails) — dry-run sample eyeballed clean, no real wine caught. `test_wines_api` green again. NOTE: `.env`'s `DATABASE_URL` points at a defunct localhost dev DB — prod DDL goes via `supabase link` + `db push` (CLI is authed) or the dashboard. FOLLOW-UP: wire `purge_non_wine.py --apply` into the weekly pipeline so re-scraped grocery noise is swept continuously.
33. ✅ **Recommender country + fortified fixes** — landed 2026-07-19. A 78209 capability sweep (structural axes: type/region/country/grape/price/compound) found two gaps: (a) fortified wines unreachable via a typed request (enum has no `fortified`, only `dessert` — item 30 retyped Port/Sherry to fortified); (b) compound country+type queries surfaced on breadth-sample luck (fetch+scorer were country-blind; `white+Argentina`→0). Fixed: `requested_types_from` folds `fortified` into a `dessert` request (one-directional); the targeted fetch (`recommend.py`) and the scorer region boost (`scorer.py`) now match the intent place against region OR country (the parser stuffs a country into the `region` field). Verified end-to-end at 78209: white+Argentina 0→27 (top-ranked), dessert requests keep both Port and Sauternes, no type+region regression. **Second capability sweep DONE 2026-07-20** (soft axes flavor/avoid/body): `avoid` was the one broken hard-exclusion path — rework landed. `wine_excluded_by_avoid` (scorer.py) is now type-aware (conservative term→wine_type map: sparkling/bubbles/champagne→sparkling, port/sherry/madeira→fortified, red/white/rosé, dessert, orange-wine/skin-contact phrases) excluding by *resolved* wine_type, with word-boundary matching over structured fields (varietal/name/grapes/region/country/flavor-tags/real-tasting_notes) — no metadata, no raw substring. Measured fixes (8k sample): "no sparkling" leaked 57%→0, "nothing fortified" 93%→0, "no dessert" 59%→0, avoid "port"→Portugal 41 false-positives→0, avoid "red" 3,616 red-fruit over-matches→0 (correctly excludes 3,127/3,127 reds). Also dropped the 100%-metadata `flavor_profile` (tokens like `PWSMigration`/`Location_SanAntonio`/`review-92plus`; `tasting_notes` 100% empty) from the flavor kw-scoring path — was producing phantom flavor matches. Body axis healthy (88% resolvable) — untouched. DEFERRED to item 34: the flavor-axis coverage gap.

34. ⚙️ **Flavor-axis coverage gap** — surfaced by capability sweep #2 (item 33). `flavor_tags_for` derives tags only from a curated grape+region map (`flavor_profiles.py`); 28% of a 6k sample get ZERO tags (flavor-invisible). But that 28% is **three distinct causes**, and only a slice is map-fixable — don't over-scope to "recover 28%":
    - **(a) ~7% of catalog — pure data gap** (no grape + no varietal + no region; nothing to look up). Not a map problem → Vivino/LLM enrichment or item 32 purge. Overlaps the region/varietal-null rows.
    - **(b) ~19% — "has a grape," but mostly junk category labels** dumped into the grape/varietal field ("Other", "Red Wine", "Red Blend", "Wine", "Sparkling Wine", "White Wine", "Sake", "Rosé/Blush"). Not real grapes → overlaps item 32 (purge) + item 30 (wine_type). Map expansion is meaningless for these.
    - **(c) the genuinely map-fixable slice (~8–10%)**: a short list of real unmapped grapes (**Moscato ~118, Glera/Prosecco ~69, Nero d'Avola ~24**) + regions (**California ~109** — map has Napa/Sonoma but not the catch-all — **Veneto ~78, Sicily, Champagne, Loire, Alsace, Penedès**). **✅ LANDED 2026-08-18** (`docs/superpowers/specs/2026-08-18-flavor-axis-map-fix-design.md`): `GRAPE_FLAVORS` gained Moscato/Glera/Prosecco/Nero d'Avola, `REGION_FLAVORS` gained California/Champagne/Alsace, and `FLAVOR_VOCAB` (+ the parser's prompt string) gained the 3 matching words — floral, citrus, mineral — needed to make those entries reachable. Veneto/Sicily/Loire/Penedès were deliberately left unmapped at the region level (each spans stylistically incompatible wines under one label — e.g. Veneto = light Soave whites AND big Amarone reds AND Prosecco — so a region-wide tag set would overclaim); Veneto and Sicily still get partial coverage indirectly via the Glera and Nero d'Avola grape entries.

    Also: the parser clamps `flavors` to a **vocab** (`FLAVOR_VOCAB`, now 18 words after landing floral/citrus/mineral above), so most other common asks (buttery, oaky, smoky, jammy, tropical) are still dropped before scoring, and the kw-hit fallback is dead (`tasting_notes` 100% empty, `flavor_profile` metadata dropped in item 33). The map+vocab pairing this batch needed is done; the rest of the vocab gap (buttery/oaky/smoky/jammy/tropical) is still open and wasn't in scope — those words aren't defensible for anything landed here. (a) and (b) remain open, belonging to enrichment + item 32 purge. Conservative rule stands — an asserted flavor tag must be defensible, not guessed.

38. ✅ **Browser-back session reset (recommend → dossier → back)** — FIXED 2026-08-01 (both parts landed; PreferenceCapture stamps a per-run `reqId`, ChatRecommend restores from the patched history entry or the `somm_chat_cache` sessionStorage entry when the reqId matches, and only auto-fires on a genuinely new submission). Original diagnosis: the in-app ← restores the chat via `_restored`, but the BROWSER back button pops to the original `/recommend` history entry (state = `{prefs, apiReq}` only), so ChatRecommend auto-fires a fresh recommendation: thread wiped + a wasted Sonnet call + a burned rate-limit slot. Fix (two parts, both cheap): (a) in `navToWine`, `navigate('/recommend', {replace: true, state: {prefs, apiReq, _restored: chatState}})` BEFORE pushing `/wine/:id` so the history entry itself carries the session; (b) sessionStorage cache keyed by `sessionId`, checked on mount before auto-fire — also survives pull-to-refresh and PWA process restarts. The ask face needs the same treatment (its chatState already carries `mode`/`askZip`/`storeRef`).

37. ✅ **Aisle-mode backend deltas** — landed 2026-07-30, from the design handoff `frontend/design-system/handoffs/aisle-mode/` (frontend build pending). (a) **Multi-bottle comparison**: intent `wine_names: List[str]` (scalar `wine_name` synced both ways, mirroring regions/region); the "named" deep fetch runs per name (cap 3) and `pin_comparison_matches` pins each bottle's best 2 to the shortlist front in named order; `comparison_wines` directive makes Somm address BOTH by name and give a verdict. Verified live: "caymus or bonanza cabernet?" parses both; 78209 fetch pins Caymus+Bonanza rows 1–3. (b) **Structured `store_ref`** on `RecommendRequest` — `resolve_requested_store` honors the aisle store picker's choice over free-text `detect_store` (non-nearby refs ignored, falls back to detection). (c) **Wide-budget sentinel** (`recommendation/budget.py`, `budget_max ≥ 1000` = unstated): scorer budget axis silent (else 0.85×$10k targets trophy bottles), prompt swaps the Budget line for "No budget was given — never mention budget…", availability lines drop "in budget" phrasing; a spoken "under $20" re-tightens via `merge_intent` and re-states the budget. **Frontend SHIPPED 2026-08-01** (plan: `docs/superpowers/plans/2026-08-01-aisle-mode-frontend.md`): PLAN A BOTTLE | ASK header tabs (mobile TopBar), Door-2 aisle strip on `/`, ask face in ChatRecommend (no-prefs arrival = Ask, empty state + 4 intent pills, wide-budget sends), lazy in-conversation zip (asked once, only when localStorage has no real zip), in-thread store picker (new `GET /api/stores/nearby`) + editable context pills → `store_ref`, two-bottle `CompareFrame` (picks event now carries `comparison` + per-pick `body`/`structure_profile` — axes are 0–10, same scale the dossier divides by 10), no-card closer offer, in-store failure states (apology in character, question preserved, "Ask again"/"Finish the answer"). Live-verified: "caymus or bonanza" over SSE returns both bottles + comparison flag. Deferred: swipe between faces, strip dismissal, No-signal top-bar readout, desktop mode tabs, comparison food row. **Rate limit DECIDED 2026-07-30: keep 15/hr/IP** — contrary to the handoff's assumption; measure how much it throttles real aisle sessions during beta before raising (blocks log + Slack-ping since `0c31c16`). Acceptance: `scripts/verify_comparison_fetch.py`.

36. ✅ **Named-retailer requests** — landed 2026-07-29. "Anything from HEB?" only *filtered the already-fetched pool*, so when no H-E-B wine happened to be in it Somm said "nothing from H-E-B turned up" — though H-E-B near 78230 stocks 300+ in-budget reds. There was a targeted fetch for region/store/named-bottle but none for a retailer chain. Fixed: `detect_retailer` (`candidate_filters.py`) is data-driven off the *nearby* retailers with punctuation-insensitive per-token aliasing (`heb`/`HEB`/`h-e-b`/`h.e.b` → H-E-B; cm→Central Market; typo-tolerant) and can never return a non-nearby chain; `_retailer_rows` fetches that retailer's nearby-store inventory and merges it before filtering; retailer-only picks when it has fits, alternatives when it doesn't; narrative now says either "every listing is from {R}" or "{R} doesn't stock a wine matching your profile" instead of the ambiguous "nothing turned up in this search". Verified: all 4 H-E-B spellings resolve, 300 H-E-B reds surface at 78230. Details: `docs/reference/recommendation.md`. Also fixed (same day): a retailer-named follow-up now yields **fresh cards**. Follow-ups ride `conversational: true` and Claude decides cards-vs-text via `_FOLLOWUP_CONVERSATIONAL_DIRECTIVE`; "anything from heb?" read as a question about the on-screen wines, so no cards appeared. There is no frontend re-ask heuristic to fix (`naturalChatMode` is just a global flag) — instead the has-fit retailer directive now states it deterministically ("This IS a request for different wines: return picks; do NOT answer with picks: []"). The no-fit branch stays conversational by design.

35. ✅ **Multi-region comparison queries** — landed 2026-07-23. A prompt naming two places ("a cab from California vs a Mendoza one") parsed to a single `region`, dropped the second, and Somm falsely said "nothing from Mendoza surfaced" (397 were in stock). Fix: intent `regions: List[str]` (scalar `region` kept in sync); `_targeted_rows` fetches each place separately; scorer `want_regions` credits any named place; `ensure_region_representation` pins each named place's best candidate into the top-12; narrative directive ("recommend one from each") when `comparison_regions` ≥2. Verified end-to-end at 78209: "California vs Mendoza" → both represented (was 0 Mendoza). Details: `docs/reference/recommendation.md`. LIMITATION (deferred): string-containment matching, so "California" ≠ "Napa Valley" (needs a place→sub-region gazetteer). **Budget-firmness DONE 2026-07-23**: an "up to $50" request was returning ~$16–20 bottles (budget targeted 0.75×max at weight 1.0 — too soft, cheap high-rated wines dominated). Reworked to target **0.85×max** (~$42.50 for $50) at weight **1.5** (still below grape 2.0/type 3.0, so a standout value wine can override — kept soft). Verified at 78209: top-8 red picks median ~$37 (was ~$16–20), with a $30–49 spread. Details: `docs/reference/recommendation.md`.

39. ✅ **Availability oracle — false absence is now structurally impossible** — landed 2026-07-30 (audit plan item #2 + #3's tripwire half; full audit: `docs/recommendation-architecture-audit.md`). Six reported "Somm says X isn't available" incidents shared one root cause: the shortlist is a bounded, ranked, diversity-capped *sample*, so it can answer "what should I recommend?" but structurally cannot answer "what exists nearby?" — and the prompt let Claude answer the second from the first. Fixed by making absence a **counted fact**: new `recommendation/availability.py` turns the constraints the user literally named (place/grape/type/name, plus a scoped copy per named retailer or store) into ≤6 axes, counts each in parallel against FULL nearby inventory — bypassing row limits, budget, the staleness window, the enrichment gate and the type gate — and derives one of five states (`UNMEASURED` > `NOT_IN_CATALOG` > `PRESENT_OUT_OF_BUDGET` > `PRESENT_SHORTLISTED`/`PRESENT_NOT_SHORTLISTED`). The predicates are deliberately BROAD by design (union over region/sub_region/country/varietal/name, both grape jsonb casings, the `rose`/`rosé` enum drift): over-counting merely declines to claim absence, under-counting reproduces the bug. `recommend.py` counts early and finalizes states against the final `top`; `claude_client.py` renders a `[VERIFIED AVAILABILITY]` block with three licence rules — **`NOT_IN_CATALOG` is the only state that licenses an absence sentence**, any listing matching a named constraint MUST appear in the picks, never contradict a verified fact — and the old denial phrase ("nothing matching that turned up nearby") is deleted from `_SYSTEM_PROMPT`. The two ad-hoc flags it supersedes (`named_bottle_found`, `retailer_has_fit`) are retired at producer, consumer and tests. Detection net: `_false_absence_suspects` (pure fn) runs post-`[DONE]` in its own try/except — absence language + any `PRESENT_*` fact → `FALSE_ABSENCE_SUSPECT` log + Slack alert; it can never break or delay a stream. Verified against live 78209 inventory (`scripts/verify_availability_oracle.py`): the bug-#6 case "Barolo at Geraldine's" → `PRESENT_OUT_OF_BUDGET`, 3 nearby, 0 in budget, $59–$110 (was a flat denial); unscoped Barolo → `PRESENT_NOT_SHORTLISTED` (56 nearby, 15 in budget); a nonsense region → `NOT_IN_CATALOG`. Fast suite 680 passing at landing. Details: `docs/reference/recommendation.md`. Hardened + verified in item 40.

40. ✅ **False-absence hardening — verified in production** — 2026-07-29/30, after item 39. Two adversarial red-team sweeps against the live deploy (`docs/oracle-production-verification.md`, `docs/oracle-verification-round2.md`) proved the oracle alone wasn't enough and drove four follow-on fixes. Red-team PASS 50%→85%, `UNUSED_FACT` 6→0, and the last confirmed false absence eliminated.
    - **Prompt: facts outrank the listings.** Round 1 found the oracle correct but the model still writing "the listings here run well above $50" against a `PRESENT_NOT_SHORTLISTED` fact of 4 in budget. The rules forbade the *sentence* but never said a fact OUTRANKS the shortlist when they disagree; now they do, naming the hedged forms ("I'm not seeing any", "the most affordable one here"), plus a mandatory count clause when in-budget exceeds what's shown, and store-scoped counts may not be restated as "nearby".
    - **Axis extraction robustness.** `"nothing from Mendoza right?"` parsed to `region=None` → **zero axes** → the oracle never ran, and the probe graded it PASS because there was nothing to contradict — a silent fail-open exactly where absence-bait questions live. Fixed in two layers: the parser extracts entities under negative/rhetorical framing, and `catalog_terms()`/`terms_in_message()` provide a deterministic floor scanned from the raw message. **Catalog-derived, not curated:** a hand gazetteer measured 146 terms and missed 6 of 11 probe terms (Barolo, Chablis, Sancerre, Pauillac, Brunello, Vinho Verde); the catalog vocabulary (~2,300, cached 1h, bounded pagination) missed none and self-updates. Also: per-axis failure isolation (one `httpx.ReadError` previously voided *every* fact), axis dedupe, and price bounds on all `PRESENT_*` states.
    - **Deterministic availability line.** Round 2 still found 2 FALSE_ABSENCE via *adherence* failures — axis substitution (counting "Montalcino, 12" for the requested "Brunello di Montalcino, 4") and agreement with a leading false premise. A third prompt round would be pleading about a number we already have, so `availability_lines()` renders it **ourselves**: an SSE `availability` frame → a `t-eyebrow` strip under the sommelier message (both layouts). Guards: narrow-beats-broad axis suppression (kills the substitution structurally) and a computed remainder via `count_shortlisted` (the model was emitting `in_budget` labelled as the remainder).
    - **The bogus-avoid bug — the real root cause.** `"nothing from Mendoza right?"` parsed the subject into **both** `avoid` and `regions`; since `avoid` is a hard exclusion the scorer deleted **275 of 300** fetched Mendoza wines, the shortlist had zero, and the narrative then *truthfully* said "No Mendoza in sight". **We destroyed what the user asked about and reported it absent** — the red team read this as sycophancy, but the model was accurately describing a list we had sabotaged. `drop_bogus_avoid` (in `merge_intent`, so every consumer sees it) removes a term that is simultaneously a positive constraint; resolution is "trust the positive" because a false exclusion is invisible and catastrophic while a missed one is visible and mild. Zero false positives on 6 genuine preference avoids. Plus `AVOID FILTER`/`SUSPECT_AVOID` telemetry — 92% of a pool vanishing must never be silent. Verified: **Mendoza in top-12 0 → 12** (scoring 2.72–2.86 vs a 1.83 cut).
    - **Tooling:** `/health` now reports the deployed commit (verifying a release was previously impossible); `scripts/probe_absence.py` streams a live recommendation and independently computes the facts — the mechanical half of the capability suite (audit item #1). Fast suite 712 passing.
    - **REMAINING** (rest of the audit plan, `docs/recommendation-architecture-audit.md`): #1 CI + capability suite, #3 telemetry half, #4 constraint-satisfaction tier, #5 unified `filter_spec`, #6 rank-then-truncate, #7 vocabulary contract tests, #8 multi-turn intent state, #9 filter-aware fake PostgREST. Also cheap: exempt the probe harness from the 15/hr rate limiter (it cost 12 of 25 probes in round 2, and 9 of 23 in round 1).

41. ✅ **Referent loss across turns — "compare these two" forgets the wines it just recommended** (found 2026-08-01 in ASK mode, zip 37210). **Repro:** turn 1 "my sister-in-law likes Sauvignon Blanc, maybe something from Sancerre?" → Somm recommends **Avaline Organic Sauvignon Blanc** ($22, Kroger) and **Starborough Sauvignon Blanc** ($13). Turn 2 "Can we compare these two? Which do you recommend?" → *"Honest answer: neither of those two wines is in the current inventory list, so let me give you a straight comparison from memory."* Both are in stock near 37210 (**Avaline 17 rows, Starborough 4**). Somm told the truth about its input — we handed it the wrong input.
    - **Two stacked causes.** (1) **The client discards the wines from history**: `ChatRecommend.jsx` `handleSend` builds `messages.map(m => ({role: m.role, content: m.text}))` — only the prose. The `picks` attached to each sommelier message (carrying `wine_id`, name, price, retailer) are dropped, so the backend never learns which wines were shown. (2) **The backend rebuilds the candidate pool from scratch every turn** — `conversation_history` is used only for the jitter seed, Claude's narrative context and session persistence; nothing carries prior picks into the new pool. The follow-up itself parses to NOTHING (verified: `wine_name`/`regions`/`region`/`grapes`/`wine_type` all empty — "these two" is anaphoric), so no targeted fetch runs either, and the fresh unordered ~500/retailer breadth sample simply didn't contain them.
    - **Why it's not caught by items 39/40.** It is a false-absence claim by a NEW route: the availability oracle only counts axes the user *named*, and "these two" names nothing — no axis, no fact, no tripwire. Lands hardest in ASK/aisle mode, where follow-ups are inherently referential ("these two", "the first one", "that Malbec"): turn 1 works, turn 2 forgets.
    - **Fix (carry the conversation's own referents):** (a) frontend — include each sommelier message's picks (at minimum `wine_id` + name) in the `conversation_history` payload; the data is already in state and is being thrown away; (b) backend — extract prior `wine_id`s from history, fetch those rows from nearby inventory BY ID **budget-free** (they were already shown, so a slider must never hide them — same rule as `_named_fetch`), `merge_candidates` into the pool, and pin them when the message is referential so a comparison cannot lose its subjects; (c) pass the structured prior picks to Claude so "the first one" is resolvable from data, not prose.
    - **FIXED 2026-08-01** — all three parts + the probe. Frontend `historyFrom` attaches each sommelier turn's picks (`wine_id`+name) to `conversation_history` (all three send paths). Backend (`recommend.py`): `prior_picks_from_history` (ordered/deduped, last 8) → budget-free by-id `_prior_rows` fetch merged into the pool every follow-up turn; `is_referential` (conservative anaphora regex: these/those/both/either/that one/first-second-last one/which one-of-do you) triggers `pin_prior_picks` inside `_score_and_select` (cheapest row per wine, history order, cap 4 — survives both fast and deep-fetch paths, applied after the retailer filter so scoped asks keep their subjects). Prompt: history preamble now lines each sommelier turn with `[Recommended in that turn: name [wine_id: …]]` so "the first one" resolves from data. `probe_absence.py --followup` added: turn 1 → picks ride history → referential turn 2 → `_false_absence_suspects` over pseudo-facts flags any denial of a turn-1 pick. Acceptance `scripts/verify_referent_carry.py` replays the 37210 repro deterministically: both bug wines fetched by id and pinned 1–2. Post-deploy check: run the probe against prod with the original repro.


42. ✅ **Producer-identity hallucination — the model recalls what we could look up** (found 2026-08-01, ASK mode, zip 78209; landed 2026-08-01). **Repro:** "Looking for something from willow creek - close to epoch in style" → *"**Epoch is a Texas Hill Country producer** known for Rhône and Bordeaux-leaning reds."* Epoch Estate Wines is **Paso Robles** (Willow Creek District AVA — which is why the user paired those terms); the catalog itself held 4 Epoch rows, 3 labelled Paso Robles (Pogo's Wine & Spirits, Dallas, $55–$103). Same structural mistake as the false-absence class (items 39/40) — an LLM inferring what the catalog could tell it directly — this time about producer identity, not availability.
    - **Fix landed:** `recommendation/producer_facts.py`, a catalog-wide producer-region lookup rendered into the prompt as a `[VERIFIED PRODUCER]` block. **Two detection layers**, because the parser alone measured insufficient: the intent parser's `wine_name` returned `None` for the exact bug case (no bottle named, just a producer mentioned in passing), so `producer_tokens()` adds a deterministic floor over the raw message — reusing the wine-name tokenizer plus a small local `_CHAT_STOPWORDS` list, added after a dev smoke test found ordinary chat words ("close", "bold") both crowding out the real producer token in the 6-token cap and themselves substring-matching real producers ("close"→"Closerie") or marketing copy ("bold"→"Big Bold Red"), producing bogus facts.
    - **Self-validating, not a maintained vocabulary:** `summarize_producer()` only emits a fact when a token's matches look like a coherent producer — bounded row count (**≤60** — a producer has a handful, a generic word like "estate" matches thousands), **≥2** regioned rows, and the top region holding **≥50%** of them (a scattered match fails and emits nothing) — majority vote with counts, so the fact is auditable ("Paso Robles, 3 of 4 bottles we carry"), not asserted. Epoch resolved **3-of-4 → 4-of-4** after the data fix below.
    - **Catalog-wide and price-blind, both deliberate**, same lesson as the availability oracle: Epoch is Dallas-only at $55–$103 while the bug's user was in San Antonio — a nearby-scoped or budget-clamped lookup returns nothing in exactly the situation where the model reaches for recall. Fails open to `[]`, never blocks a recommendation.
    - **Hedging rule:** when a named producer appears in the `[VERIFIED PRODUCER]` block, Claude must use that region; when it doesn't (lookup found nothing or failed the bound), it may share general knowledge but must hedge ("I believe Epoch is from Paso Robles"), never assert flatly.
    - **Data defect fixed:** `Epoch Estate Wines Block B Paso Robles 2018` was mislabelled `region=Ribeira Sacra, country=Spain` despite its own name — corrected to Paso Robles/United States.
    - **Report-only contradiction sweep:** `backend/scripts/detect_region_contradictions.py` derives a place vocabulary from the catalog's own region/sub_region/country columns (self-updating, no hand gazetteer) and flags any wine whose name contains a place term absent from its own fields. Live run: **5,622 of 23,166 wines (~24%) flagged** — genuine defects mixed with expected AVA-hierarchy fuzziness (e.g. "Yountville" not being a literal substring of "Napa Valley"); per the standing conservative-enrichment rule it never auto-corrects, only reports for human review. Details: `docs/reference/recommendation.md` (Producer facts). Acceptance: `backend/scripts/verify_producer_facts.py`. Tests: `backend/tests/test_producer_facts.py` (9), 2 in `test_claude_client.py`; fast suite 756 passing at landing.

44. ✅ **Picked store now hard-constrains the recommendation (aisle mode)** — landed 2026-08-25 (plan `docs/superpowers/plans/2026-08-25-aisle-hard-store-filter.md`). Was ⬜: (found 2026-08-24 in ASK/aisle mode, prod, zip 78209). **Repro:** store picker set to **Geraldine's Natural Wines** (context pill reads `◎ Geraldine's Natural Wines · change`), asked *"something that pairs well with pizza, red wine"* → two picks: the COS Cerasuolo at **Geraldine's (0 mi)** and a **Torbreck Barossa Shiraz at Twin Liquors (4.4 mi)**, with the narrative closing *"These two hit that mark"* — no acknowledgement that one isn't in the store the user said they're standing in.
    - **The picked store never reaches the model.** `resolve_requested_store` honors the structured `store_ref` (item 37b) and `recommend.py:618-620` **sorts** that store's rows to the front of the shortlist — but it never *filters*, and nothing in the prompt states which store the user is in. The only store instruction in `claude_client.py` is *"If I named a retailer or store, only recommend wines shown at that retailer/store"*, conditioned on the user **naming** one in free text. A picker selection names nothing, so the model correctly concludes no store constraint applies and ranks purely on fit.
    - **Soft-filtering IS the intent** — the picker's own copy says *"Store is a soft filter, not a cage — I'll still name a better bottle down the road if there is one."* The defect isn't that it left the store; it's that it can't *know* it left, so it can't flag the trade the copy promises. The distance data needed to say it was already on screen (0 mi vs 4.4 mi) and unused.
    - **Same class as items 39/40/42 and the nullable-zip work:** UI chrome asserts a scope (`◎ Geraldine's · change`) that the answer silently doesn't honor. Lands hardest in aisle mode, where "I'm here, now" is the entire premise — a bottle 4.4 mi away is useless to someone standing in an aisle.
    - **Fix path:** pass the standing store into the prompt as a stated fact (not an inference from free text), and require Somm to flag any pick that isn't there — "the Torbreck's worth the 4.4 mi if you're willing" — rather than presenting mixed-store picks as one undifferentiated list. Decide explicitly whether aisle mode should default to a HARD store filter with an opt-out, since the "soft filter" copy was written for the desktop store-detection path, not for someone physically in an aisle. Consider surfacing `distance_miles` in the narrative directive so the trade is always priced.
    - **LANDED (hard filter, not soft):** decision — a picked store means the user is standing there, so a wine exclusive to another store must never appear (the picker's old "soft filter, not a cage" copy was wrong for the aisle and is rewritten). (1) `filter_to_store()` (`candidate_filters.py`) hard-filters the pool inside `_score_and_select` — the single scoring choke point, so breadth + every deep-fetch merge is covered; the old store post-sort is removed. (2) `resolved["standing_store"]` → an `[IN-STORE]` prompt directive (`claude_client.py`): recommend only from these shelves, and when nothing's a strong fit, say so and offer to check nearby rather than padding. (3) Frontend **"Check nearby stores"** affordance (`ChatRecommend.jsx`, modeled on the no-card closer) re-sends the last question WITHOUT `store_ref` — a one-turn widen; `storeRef` state persists so the next question re-scopes. **Cross-store is only reachable by omitting `store_ref`, which only that affordance does — so "no cross-store without explicitly asking" is enforced by construction.** Backend 775 / frontend 296 green. Acceptance: `scripts/verify_store_filter.py`.

45. ✅→⚙️ **H-E-B + Central Market scrapers — Incapsula JS challenge; H-E-B DEPLOYED + VERIFIED on the mini 2026-08-26, Central Market still parked** (found 2026-08-25; both failing every weekly run since 2026-07-26, last success 07-19). **H-E-B inventory is 36d old → benched (>10d stale) → San Antonio testers currently get ZERO H-E-B wines**, and H-E-B was the biggest SA source (~39k records). HEB moved `heb.com/graphql` behind **Imperva Incapsula** (~07-26). NOT the datacenter-IP block the probe first guessed — fails from residential too, so the mini won't fix it. Layers: a cold POST 401/502s; a storefront GET issues provisional `incap_ses`/`visid_incap` cookies that get a LIGHT query through (200, 1999 wines); but the real full-fields query hits a **502 Incapsula JS-challenge page** (`_Incapsula_Resource`) consistently (4/4) — Incapsula wants the client to execute its obfuscated JS for the real cookie, which curl/urllib can't. Also: Incapsula fingerprints the client (curl gets 200 on the prime GET, urllib gets 403). Cookie-priming + curl is necessary but NOT sufficient. **FIX FOUND (spike, `scripts/spike_heb_playwright.py`):** vanilla Playwright is `errorCode 15`-blocked headless AND headed; **`patchright`** (patched Playwright, strips CDP tells) with `launch_persistent_context(headless=False, no_viewport=True)` + `wait_until=domcontentloaded` + ~6s clears it, and the full query POSTed via in-browser `page.evaluate` returns 200 (HEB 1999, CM 157). BUILD: add patchright dep; rewrite HEB/CM fetch to solve once then paginate via `page.evaluate`; **must run HEADED → needs a GUI session on the mini (a launchd daemon has no display; use a GUI LaunchAgent like the Vivino job)**. **BUILT + LOCALLY VERIFIED 2026-08-25** (`scrapers/heb.py` `_BrowserSession`: one patchright persistent headed context on a dedicated worker thread — escapes run_full's asyncio loop — solves Incapsula once, POSTs via in-browser `page.evaluate`, re-solves on challenge; lazy-start + atexit close, no run_full changes; HEB+CM share it via `graphql_post`). 776 fast tests green (session retry unit-tested with a fake page); live `scripts/verify_heb_session.py` inside an asyncio loop → HEB store 567 = 1999, session reused across pages. `patchright` added to requirements. **REMAINING: (a)** deploy on the mini as a GUI LaunchAgent + move HEB/CM off the GitHub cron (queued `docs/mini-agent-tasks.md`); **(b) all configured `CM_STORES` ids now return 0** (HEB renumbered/disabled them — store 51 returns 157) → CM needs a store-id refresh before it commits any wine. **Earlier fix options (now moot unless patchright fails on the mini):** (1) headless browser + stealth to solve the JS challenge and harvest the cookie, then feed the existing curl GraphQL path — reverses the repo's "no Playwright" stance, runs on the mini; (2) anti-bot/residential-proxy service (~few $/wk); (3) accept the loss and lean on Spec's/Twin Liquors/indies. Full write-up + evidence: `data/exploration/heb_incapsula_findings.md`. Secondary: configured CM store 61 now returns 0 (not e-commerce-enabled); store 51 → 157 — revisit `CM_STORES` when restored.
    - **DEPLOYED 2026-08-26 (from the mini).** `patchright` 1.60.1 + `patchright install chromium` (nothing was cached — it pulls the headless shell AND full Chromium; only the **full** build works, headless is fingerprint-blocked). Headed confirmed: console user `danielguerrero`, `launchctl managername` = **Aqua**. `com.somm.heb` LaunchAgent runs **Sun 05:30 CT** after extraction/Twin/Spec's — deliberately **not** `ProcessType Background` and `LowPriorityIO false` (throttling stalls the Incapsula solve), wrapped in `caffeinate -i` (a mid-run sleep kills the browser session). H-E-B + CM **removed from `weekly-scrape.yml`** (a CI runner has no display, so headed can never work there) and dropped from its Slack summary; `verify_scrape_runs.py` still guards. **Live result: run 1 failed on errorCode 15 after 59,007 rows; run 2 (post-fix) SUCCESS — 62,474 records, 48 stores. H-E-B is un-benched (62,490 fresh rows) — SA testers have H-E-B back after 5 weeks.**
    - **Three defects the first live run exposed** (all fixed, `scrapers/heb.py`, 4 new tests, suite 780): (1) `_w_solve` accepted a **page title** as proof the challenge cleared — but a fingerprint block serves a normal storefront while every `/graphql` POST 401s, so the re-solve loop declared victory over a block it never touched and `_w_post` burned all 3 retries; it now **probes the API itself** (raw `evaluate`, not `_w_post` — that would recurse) and only accepts a 200. (2) No proactive re-solve: ~1k POSTs in one session trips the block, so `_w_post` now re-solves every `_RESOLVE_EVERY`=150 successful POSTs (~6s each vs. losing the run). (3) The failure path wrote `records_updated=0` for a run that had committed 59,007 rows — understating it and misleading `verify_scrape_runs.py`, which keys on exactly that number; it now records the partial count.
    - **Unrelated bug caught by the same failure:** `EXIT=$?` placed after a `{ … } >> "$LOG" 2>&1` group captures the group's trailing `echo`, not the python — so a failed run reported exit 0 and Slack was told "OK". **Twin Liquors and Vivino have done this for as long as they've run**; `run_specs`/`run_extraction` are fine (they capture inside the block). All three fixed. Their first honest Slack alert will be the next Sunday fire.
    - **Central Market still parked** (not scheduled — it would commit 0 rows and trip the silent-zero alert weekly). Blocker for the store-id refresh: the **`Apollographql-Client-Name` header is NOT a discriminator** — store 567 (a regular H-E-B) returns 1999 under *both* `heb-com` and `central-market`, so `productSearch` alone can't tell a CM store from an H-E-B one and scanning neighbouring ids just finds live H-E-B stores. Schema probing shows a root `store` field exists (it rejects `storeId` as an unknown argument) but the 400 body truncates before the "Did you mean". Next avenue: drive the already-solved **browser session** to centralmarket.com's store locator and read ids off the page. Details: `docs/mini-agent-tasks.md`.

43. ✅ **Nullable user location — stop fabricating `78209`** (merged 2026-08-24, PR #3; plan/spec `docs/superpowers/{plans/2026-08-21,specs/2026-08-20}-nullable-zip*`). Every screen that filters inventory needs "where is the user?", and each had grown its own precedence chain from whatever navigation state its entry path carried; when nothing was known the code fabricated `78209` and rendered it as confirmed. Two live bugs from that: the dossier's "available near you" listed **17 stores nationwide** (a Spec's 252 mi away) because from the ASK face the chain hit `null`, `getWine` dropped `?zip=`, and the API skipped its proximity filter — while the header pill showed the right zip from a *different* source; and RegionBrowse hard-coded `78209` so a Nashville user silently saw San Antonio inventory. **Fix:** `loadZip()` returns `null` (not a default) when nothing is stored; one shared `useUserZip()` resolver (`frontend/src/lib/useUserZip.js`) owns the precedence chain — no screen builds its own; and every screen renders an explicit no-location state (header **"Set location"** not `◎ 78209`; Deals/Search/Region/Discovery prompt for a location instead of firing zip-required endpoints or claiming "near you"; RegionDetail counts read **"in the catalog"** vs "nearby"; dossier **"Where to find it"** vs "Available near you"; zip inputs stay controlled when null; ASK asks for a zip *before* the store picker). Full frontend suite 294/294 green.
    - **Two supporting fixes merged alongside** (surfaced while getting the branch review-ready): PR #1 `fix/ask-tabs-stateless` — the header's `askMode` didn't match `ChatRecommend`'s, so a reload/direct-load of `/recommend` rendered the ask face but dropped the PLAN|ASK tabs (showed "Tonight"); now mirrors it exactly. PR #2 `fix/test-setup-localstorage` — the Node 22+ `localStorage` test gotcha above. Both were pre-existing (not caused by nullable-zip), independent, and merged first so the feature's CI ran honestly green.

46. ⚙️ **Total Wine — UNLOCKED 2026-08-26 (access + store scoping solved); blocked only on HTML product parsing** (full evidence: `data/exploration/totalwine_findings.md`; probe `backend/scripts/probe_totalwine_store.py`). Probed on the theory that Imperva *is* Incapsula, so item 45's patchright fix should transfer. **It doesn't — and the reason is the useful finding.**
    - **The block profile is INVERTED vs H-E-B.** Plain `urllib` gets **200** on the storefront (homepage 375KB, `/wine/c/c0020` 1.43MB, no `_Incapsula_Resource`, no challenge). The **patchright browser gets 403** on `/store-finder` — with a control urllib request in the same minute returning 200/854,808 bytes, so it is fingerprinting, not rate-limiting. H-E-B: HTTP blocked, browser works. Total Wine: HTTP works, browser refused. **A browser here is a liability**; any scraper must be HTTP-only. (Caveat: probed from the mini's residential IP only — re-probe from a datacenter IP before assuming CI can run it.)
    - **The catalog is reachable.** Products are server-rendered into category HTML (79 prices on one page). CSS-module classes are hashed (`price__ff218822`) and will churn every frontend build, but the **product URL is a stable anchor and encodes the taxonomy**: `/wine/red-wine/cabernet-sauvignon/caymus-cabernet/p/223968750` → `/{cat}/{color}/{varietal}/{slug}/p/{productId}`. No JSON catalog: `/site/resourceapi/` is **Bloomreach CMS** page-delivery (`"product":"brxm"`), and the category page's component model returns 9KB with **zero** `price`/`productId`/`sku`/`inventory`.
    - **THE BLOCKER — store scoping.** Scoping exists (`{"shoppingMethod":"INSTORE_PICKUP","storeId":"1108","state":"US-CA"}`, plus `cacheStoreId`/`cacheState` on CMS URLs) but defaults to a **California** store, and guessed cookie names had zero effect (`US-CA` vs `US-TX` returned byte-identical 1,431,627-byte responses). **This is disqualifying, not annoying:** the app's premise is "available near you," so California prices would feed the recommender confident wrong inventory — precisely the false-availability class items 39/40/44 exist to prevent. Don't build this until scoping is proven.
    - **Store ids are enumerable, not guessable:** `/store-info/{state}-{city}/{storeId}` (e.g. `/store-info/california-sacramento-arden/1108`). The finder geo-defaults to CA and renders no Texas cities, but `robots.txt` lists 3 sitemaps (`sitemap.xml` indexes 34 sub-sitemaps) — harvest `texas-*` from there. Probing **stopped on a 503** from `site/sitemap-index.xml` after a sustained run; resume gently.
    - **ToS signal (not legal advice):** `robots.txt` **disallows `/search/`** (also `/cart/`, `/checkout/`, `/my-account/`, `/session`, `/app/`) but leaves `/wine/…` category, `/p/{id}` product and `/store-info/` paths crawlable. Design constraint: **browse categories and store-info; never drive `/search/`.**
    - **SOLVED — store selection is one forgeable cookie.** `twm-userStoreInformation=ispStore~{id}:ifcStore~{id}@ifcStoreState~{US-XX}@method~INSTORE_PICKUP`, sent on a plain `urllib` GET. No session or handshake — visiting a store-info page does NOT rebind it, so forging the cookie is both necessary and sufficient. **Go/no-go gate PASSED** on `/wine/c/c0020`: the echoed `storeId` follows what you ask for (1108→503→504), byte counts move (1,430,774 / 1,463,627 / 1,464,253), and prices genuinely differ — position 3 is $10.99 in Sacramento vs **$12.99** in San Antonio, position 7 $12.49 vs **$12.99** — with the two SA stores agreeing with each other and differing from CA, the signature of real regional pricing. **San Antonio store ids: 503 Del Norte, 504 The Rim, 520 Forum.** Harvest route for any market: `local_delivery_pages_sitemap.xml` (205 urls, 34 Texas) → `alcohol-delivery-near-me-{City}-{State}` → scrape `/store-info/{state-city}/{id}`.
    - **ONLY REMAINING BLOCKER — product identity parsing.** Prices extract cleanly but the `href` regex found **no** `/p/{productId}` matches in the gate test (an earlier pass got only 51 of 613 `href=` occurrences). Prices without a product key are useless. This is a parsing problem, not an access one: anchor on `/p/{digits}`, treat the hashed CSS classes as unusable, and confirm the product↔price pairing survives the SSR markup.
    - **NEXT STEPS (HTTP only — do not reach for a browser):** (1) harvest TX store ids from the sitemaps; (2) fetch a Texas `/store-info/{…}/{id}` and diff response headers against the California one — whatever binds a store to a session (a `Set-Cookie`, a form POST, a sticky query param) should be visible in a plain request; (3) **GO/NO-GO GATE: prove prices actually move** — fetch the same category under a CA store and a TX store and compare a known product (`caymus-cabernet/p/223968750`); if the price doesn't change, scoping isn't real and the effort stops there, before any scraper is written; (4) only then cost the SSR HTML parse anchored on `/p/{productId}`; (5) re-probe from a datacenter IP to decide CI vs mini.
