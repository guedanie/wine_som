{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "true",
  "rewrites": [
    { "source": "/((?!assets|icons|manifest.json).*)", "destination": "/index.html" }
  ]
}
